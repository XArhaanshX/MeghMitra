// Mirrors ankur_domain.policies.MIN_AUTO_ELIGIBLE_CONFIDENCE -- display-only threshold,
// never re-implement the actual eligibility check client-side.
export const MIN_AUTO_ELIGIBLE_CONFIDENCE = 0.85;
